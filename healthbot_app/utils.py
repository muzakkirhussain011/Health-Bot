def count_words(message):
    # Remove leading and trailing whitespaces
    # message = message.strip()

    # # Split the message into words
    # words = message.split()

    # # Count the number of words
    # count = len(words)

    # return count
    area = int(message) * int(message)
    return area